SUFFIXES = .moc.cpp

.h.moc.cpp:
	$(QT_MOC) -o $@ $<

# this is now in each of the Makefile.am files because they also have
# their own clean-local target!
#clean-local:
#	if [ -n "`ls *\.moc\.cpp`" ];then rm `ls *\.moc\.cpp`;fi
#
