PLUGINS

Build Includes, and folders

* user plugins will need to include from the system folder
  OR will need to have their own complete copy of the system
  .pre include files
  
Configure

* need to get:
  variant (mpi, etc.)
  version (4.0.2 etc.)
  
Build

Install

Dependencies

* when configure changes, we really want to invalidate all the deps,
  so that the plugins rebuild
  
WIZARD

* need a general framework for wizards
  * outer dialog
  * pages
  * page navigation, including navigability constraints
  *
  