dnl Process this file with autoconf to produce a configure script.

dnl AC_INIT is the first macro that should be called in an Autoconf
dnl script. It should be called with one argument that exhibits one
dnl of the source files
AC_INIT(src/ArmModel.cpp) dnl a source file from your sub dir

dnl Automake doc recommends to do this only here.
AM_INIT_AUTOMAKE(armmodel, 0.1)

dnl Initialize libtool
AM_PROG_LIBTOOL

dnl Checks for programs.
AL_PROG_CC
AC_PROG_CXX
AC_PATH_XTRA

dnl Checks for libraries.
AC_CHECK_LIB(m,pow)
AC_CHECK_LIB(pthread, pthread_create)

dnl Checks for header files
AC_HEADER_STDC
AC_CHECK_HEADERS(sys/time.h unistd.h stdint.h)

dnl create only shared libtool-libraries
AC_ENABLE_SHARED(yes)

dnl set the following to yes, if you want to create static
dnl libtool-libraries, too.
AC_ENABLE_STATIC(no)

dnl ************ Start project specific libraries *************
AC_CHECK_HEADERS(gazebo.h,,
                 [AC_MSG_ERROR([cannot find gazebo.h, bailing out])])

dnl ************ End project specific libraries
AC_SUBST(AUTODIRS)
AC_OUTPUT([Makefile src/Makefile])

AC_MSG_RESULT([])
AC_MSG_RESULT([*************************************************************])
AC_MSG_RESULT([])
AC_MSG_RESULT([libarmmodel will be built on a $build system to run])
AC_MSG_RESULT([on a $host system, with the following tools:])
AC_MSG_RESULT([    C++ compiler: $CXX $CXXFLAGS])
AC_MSG_RESULT([])
AC_MSG_RESULT([To load the plugin modules make sure to set GAZEBOPATH.])
AC_MSG_RESULT([If you use bash, add the following to your .bashrc file:])
AC_MSG_RESULT([])
AC_MSG_RESULT([  export GAZEBOPATH=/usr/local/lib:/usr/lib:$GAZEBOPATH:])
AC_MSG_RESULT([])

